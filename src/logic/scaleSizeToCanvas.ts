export function widthToCanvas(width: number, worldSizeX: number) {
    return width * worldSizeX;
}

export function heigthToCanvas(heigth: number, worldSizeY: number) {
    return heigth * worldSizeY;
}
